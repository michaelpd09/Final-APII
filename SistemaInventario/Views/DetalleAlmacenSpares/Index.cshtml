@model IEnumerable<SistemaInventario.Models.DetalleAlmacenSpares>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AlmacenId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SpareId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AlmacenId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SpareId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DetalleAlmacenSparesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DetalleAlmacenSparesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DetalleAlmacenSparesId })
        </td>
    </tr>
}

</table>
